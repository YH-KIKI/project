<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.hi.project.dao.DietAnalysisDao">

    <select id="selectDailyTotalNutrients" resultType="map">
        SELECT 
            COALESCE(SUM(f.fo_kcal * md.md_portion), 0) as total_kcal,
            COALESCE(SUM(f.fo_carbs * md.md_portion), 0) as total_carbs,
            COALESCE(SUM(f.fo_protein * md.md_portion), 0) as total_protein,
            COALESCE(SUM(f.fo_fat * md.md_portion), 0) as total_fat,
            COALESCE(SUM(f.fo_natrium * md.md_portion), 0) as total_sodium
        FROM meal_log ml
        JOIN meal_detail md ON ml.mk_num = md.mk_num
        JOIN food f ON md.fo_num = f.fo_num
        WHERE ml.user_num = #{userNum} 
          AND DATE(ml.mk_created_at) = #{date}
    </select>

    <select id="selectUserTargetKcal" resultType="java.lang.Integer">
        SELECT up_daily_kcal 
        FROM user_privacy 
        WHERE user_num = #{userNum}
    </select>

    <update id="updateCharacterExp">
        UPDATE `character` 
        SET ch_exp = ch_exp + #{earnedXp} 
        WHERE user_num = #{userNum}
    </update>
    
    <select id="selectRecordedDates" resultType="string">
	    SELECT DISTINCT DATE(mk_created_at)
	    FROM meal_log
	    WHERE user_num = #{userNum}
	    ORDER BY DATE(mk_created_at) DESC
	</select>

</mapper>