<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hi.project.dao.ChatbotDao">

    <select id="getUserChatbotMode" resultType="string">
        SELECT T.ct_speaking_style 
        FROM chatbot_config C
        JOIN chatbot_type T ON C.ct_num = T.ct_num
        WHERE C.user_num = #{userNum}
    </select>

</mapper>